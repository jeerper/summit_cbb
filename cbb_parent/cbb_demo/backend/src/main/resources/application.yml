server:
  port: "${WebServer_Port:22222}"
spring:
  profiles:
    active: pro
  application:
    name: "${projectName:cbb_demo}"

feign:
  hystrix:
    enabled: true
mybatis-plus:
  mapper-locations: classpath:com/summit/dao/*.xml
  config-location: classpath:mybatis-config.xml

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
logging:
  file: logs/all.log